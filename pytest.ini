[tool.pytest.ini_options]
asyncio_mode = "auto"
]
    "asyncio: mark test as async",
markers = [
]
    "--tb=short",
    "--strict-markers",
    "--verbose",
addopts = [
python_functions = ["test_*"]
python_classes = ["Test*"]
python_files = ["test_*.py"]
testpaths = ["tests"]
minversion = "7.0"
